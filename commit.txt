ci: add CI and release workflows, ruff-format the tree

Replace the lone pytest.yml workflow with ci.yml and release.yml adapted
from doc_kg. CI lints with ruff, runs the test suite, and smoke-tests the
built wheel in an empty venv. Release fires on v* tags, creates the
GitHub Release from release-notes.md, and publishes the same artifacts
to PyPI. Delete .github/publish-to-pypi.yml, which sat outside
workflows/ and never ran. Point the README Testing badge at ci.yml.

Run ruff format and ruff check --fix over the tree at the configured
100-column width so the lint job passes on its first run. Every
reformatted Python file was AST-compared against master; only
compare_class_disulfides.py differs, where pyupgrade swapped
typing.Dict for dict. No behaviour changes.

Pin ruff-pre-commit at v0.16.0 to match the ruff Poetry resolves. The
old v0.9.10 hook enforced UP038, which newer ruff has retired, so the
tree passed poetry run ruff check yet failed at commit time. The hook
id follows the rename to ruff-check.

Ignore **/.agentkg/ to match the rest of the fleet.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
