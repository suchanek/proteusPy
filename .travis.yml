language: generic
dist: xenial
before_install:
  - sudo apt-get update
  - sudo apt-get install -y python3.11.7

install:
    - pip install git+https://github.com/suchanek/biopython.git@egs_ssbond_240305#egg=biopython
    - pip install pytest
    - pip install .
script:
    - pytest